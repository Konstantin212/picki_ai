#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run custom pre-commit checks for empty files/folders and unused variables
node scripts/pre-commit-checks.js

# Run lint-staged for formatting and linting
pnpm lint-staged
